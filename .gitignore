# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Every .env variant except the committed template. The previous two lines
# missed .env.local and .env.production, which Vite reads happily.
.env
.env.*
!.env.example

# Supabase CLI local state
supabase/.temp
supabase/.branches

# Test output
coverage
playwright-report
test-results
# Local edge function secrets
supabase/functions/.env

# Database dumps. These contain real student emails, bcrypt password hashes and
# session tokens - they are operational artefacts, not source, and must never
# reach a remote. Keep them locally or in a private store.
supabase/backups/*.sql

# The repo root is a Vite/Node project, not a Deno one. Running `deno` from
# here (or installing it) drops a lockfile that has nothing to do with the
# edge functions, whose own lock lives in supabase/functions/.
/deno.lock

# The scaffold left a bun lockfile behind. This project installs with npm.
bun.lockb
